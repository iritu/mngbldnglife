import { Button, Col, Container, Row } from 'react-bootstrap';

function Tickets(){
    return (
        <Container>
            <h1>Tickets</h1>
        </Container>
    )
}

export default Tickets; 
